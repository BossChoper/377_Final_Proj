{
  "version": 2,
  "builds": [
    {
      "src": "index.js", // Or your main server file (e.g., server.js, app.js)
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
